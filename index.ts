// Importing the necessary function to register the root component from Expo
import { registerRootComponent } from 'expo';
// Importing the main App component that will be rendered as the root component
import App from './App';

// Registering the App component as the root component to run the app
registerRootComponent(App);
